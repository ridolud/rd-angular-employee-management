export class Employee {
  username?: string;
  firstName?: string;
  lastName?: string;
  email?: string;
  birthDate?: Date;
  basicSalary?: number;
  status?: 'active' | 'non-active';
  group?: string;
  description?: string;
}
